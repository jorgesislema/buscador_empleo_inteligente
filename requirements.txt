# === Dependencias del Proyecto: Buscador de Empleo Inteligente ===
# Este archivo lista todos los paquetes Python externos necesarios.
# Podemos instalar todo con: pip install -r requirements.txt

# --- Para la Configuración ---
PyYAML>=6.0         # La usamos en config_loader.py para leer nuestro settings.yaml. ¡Fundamental!
python-dotenv>=1.0.0 # La necesitamos para cargar las variables del archivo .env (nuestras API keys!).

# --- Para Hacer Peticiones Web (APIs y Scrapers) ---
requests>=2.28.0    # ¡El pan de cada día! Con esta librería hacemos las llamadas HTTP a las APIs y descargamos el HTML de las páginas web.

# --- Para Parsear HTML (Web Scraping) ---
beautifulsoup4>=4.11.0 # La estrella para navegar y extraer datos del HTML que descargamos con requests. Facilita mucho la vida.
lxml>=4.9.0           # Este es el 'parser' que beautifulsoup suele usar por debajo. Es rápido y robusto. ¡Buena combinación!

# --- Para Manipulación de Datos (Opcional pero muy útil) ---
pandas>=1.5.0         # Aunque quizás no la usemos al principio, Pandas es casi indispensable si queremos analizar los datos, guardarlos fácil en CSV, o hacer limpiezas complejas. La añadimos ya.

# --- Para Programar Tareas (Scheduler Opcional) ---
schedule>=1.1.0       # Si queremos que el script se ejecute automáticamente cada cierto tiempo (ej. una vez al día), esta librería es sencilla y útil.

python-dotenv
